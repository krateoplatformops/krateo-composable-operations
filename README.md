# Repository ready to use

This repository has been configured with:

- precommit hooks
- semantic release

> REMINDER: create the secret required by the GitHub Actions for semantic release plugin:
>
> - GH_TOKEN
>
